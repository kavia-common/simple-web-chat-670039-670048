# Backend REST base URL (Create React App)
REACT_APP_BACKEND_HTTP_URL=

# Backend WebSocket URL (Create React App)
REACT_APP_BACKEND_WS_URL=

# For Vite-based builds (not required for CRA but included for portability)
VITE_BACKEND_HTTP_URL=
VITE_BACKEND_WS_URL=
